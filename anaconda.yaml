anaconda_config_version: 1
upstream_sources:
  - github:
      identifier: openSUSE/libsolv
      use_max_tag: true
      include_regex: ^\d+\.\d+\.\d+$
    packages:
      - libsolv
      - libsolv-static
